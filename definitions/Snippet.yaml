type: object
properties:
  id:
    type: integer
    format: int32
  name:
    type: string
  applications:
    type: string
    items:
      $ref: "#/definitions/SnippetApplication"
  arguments:
    type: array
    items:
      $ref: "#/definitions/SnippetArgument"