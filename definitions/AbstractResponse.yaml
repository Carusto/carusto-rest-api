type: object
discriminator: responseType
required:
  - success
  - code
  - message
  - responseType # required for inheritance to work
properties:
  success:
    type: boolean
  code:
    type: string
  message:
    type: string