type: object
properties:
  inbound:
    type: array
    items:
      $ref: "#/definitions/GatewayNormalizationRule"
  outbound:
    type: array
    items:
      $ref: "#/definitions/GatewayNormalizationRule"