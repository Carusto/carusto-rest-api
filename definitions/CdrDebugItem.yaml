type: object
properties:
  uid:
    type: string
  thread:
    type: string
  time:
    type: integer
    format: int64
  logs:
    type: array
    items:
      $ref: "#/definitions/CdrDebugLog"
  application:
    $ref: "#/definitions/CdrDebugApplication"
