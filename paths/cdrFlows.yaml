get:
  tags:
   - cdr
  security:
   - basicAuth: []
  parameters:
    - $ref: "#/parameters/cdrCallId"
  responses:
    200:
      description: Successful response
      schema:
        type: array
        items:
          $ref: "#/definitions/CdrFlow"