get:
  operationId: getCallHistoryFlows
  tags:
   - calls
  parameters:
    - $ref: "#/parameters/cdrCallId"
  responses:
    200:
      description: Successful response
      schema:
        $ref: "#/definitions/CdrFlowsResponse"