post:
  operationId: executeAsync
  tags:
   - cml
  security:
   - basicAuth: []
  parameters:
    - $ref: "#/parameters/cmlData"
  responses:
    200:
      description: Successful response
      schema:
        $ref: '#/definitions/AsyncResponse'
