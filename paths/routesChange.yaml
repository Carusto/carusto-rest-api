put:
  operationId: updateRoute
  tags:
   - routes
  security:
   - basicAuth: []
  description: |
    Gets `Route` objects.
  parameters:
    - $ref: "#/parameters/routeId"
    - $ref: "#/parameters/routeName"
    - $ref: "#/parameters/routeStatus"
    - $ref: "#/parameters/routePriority"
    - $ref: "#/parameters/routeApplications"
    - $ref: "#/parameters/routeConditions"
  responses:
    200:
      description: Successful response
      schema:
        $ref: '#/definitions/RouteResponse'
delete:
  operationId: deleteRoute
  tags:
   - routes
  security:
   - basicAuth: []
  description: |
    Gets `Route` objects.
  parameters:
    - $ref: "#/parameters/routeId"
  responses:
    200:
      description: Successful response
      schema:
        $ref: '#/definitions/AbstractResponse'