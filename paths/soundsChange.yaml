delete:
  operationId: deleteSound
  tags:
   - sounds
  parameters:
    - $ref: "#/parameters/soundId"
  responses:
    200:
      description: Successful response
      schema:
        $ref: '#/definitions/AbstractResponse'