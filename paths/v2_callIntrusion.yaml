post:
  operationId: intrusionCall
  tags:
   - calls
  parameters:
    - $ref: "#/parameters/idInPath"
    - $ref: "#/parameters/callDialMethod"
    - $ref: "#/parameters/callDialNumber"
    - $ref: "#/parameters/callDialToken"
  responses:
    200:
      description: Successful response
      schema:
        $ref: '#/definitions/GenericResponse'